# Northstar is intentionally stdlib-only (Python 3.11+).
# No third-party packages are required to collect, analyze, or render.
# Optional: install nothing. If you later add a key-based provider, put it here.
